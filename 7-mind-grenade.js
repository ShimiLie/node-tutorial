const num1 = 5;
const num2 = 10;

addValues = () => {
  console.log(`The sum of num1 and num2 is ${num1 + num2}`);
};

addValues();

//linked to 3,
//the module can exported out and function will be invoked
